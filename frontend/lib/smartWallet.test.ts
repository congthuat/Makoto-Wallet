import assert from "node:assert/strict"; import test from "node:test"; import { isSponsored, smartWalletState } from "./smartWallet.ts";
test("smart wallet capability detection is truthful", () => { assert.equal(smartWalletState(), "configuration-required"); assert.equal(smartWalletState({ available: true, sponsorship: false, batching: false }), "experimental"); assert.equal(smartWalletState({ available: true, sponsorship: true, batching: false }), "available"); assert.equal(isSponsored({ available: true, sponsorship: false, batching: true }), false); assert.equal(isSponsored({ available: true, sponsorship: true, batching: false }), true); });
