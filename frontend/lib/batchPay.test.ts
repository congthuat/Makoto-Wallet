import assert from "node:assert/strict"; import test from "node:test"; import { batchPayCapability, batchTotal, buildBatchTransferCalls, validateBatch } from "./batchPay.ts";
const a = "0x1111111111111111111111111111111111111111"; const b = "0x2222222222222222222222222222222222222222"; const token = "0x3600000000000000000000000000000000000000";
test("batch validation covers totals, duplicates, addresses, balance and size", () => { assert.deepEqual(validateBatch([{ recipient: a, amount: 1n }, { recipient: b, amount: 2n }], 3n), []); assert.equal(batchTotal([{ recipient: a, amount: 1n }, { recipient: b, amount: 2n }]), 3n); assert.ok(validateBatch([{ recipient: a, amount: 1n }, { recipient: a, amount: 2n }], 2n).includes("duplicate-recipient")); assert.ok(validateBatch([{ recipient: "bad", amount: 0n }], 2n).includes("invalid-recipient")); });
test("batch call generation is exact and execution fails closed without verified Multicall3From", () => { assert.equal(buildBatchTransferCalls(token, [{ recipient: a, amount: 1n }])[0].target, token); assert.equal(batchPayCapability().state, "configuration-required"); assert.equal(batchPayCapability(a).state, "supported"); });
